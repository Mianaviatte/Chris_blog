<h1><%= @article.title %></h1>
 
<p><%= @article.body %></p>

<h2>Comments</h2>
<%= render @article.comments %>

<h2>Add a comment</h2>
<%= render 'comments/form' %>

<ul>
    <li><%= link_to "Edit form.", edit_article_path(@article) %> Edit these qoutes.</li>
    <li><%= link_to "Delete!", article_path(@article), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %> Delete these qoutes.</li>
    <li><%= link_to "Home.", root_path %> Go back to the list of quotes.</li>
</ul>
